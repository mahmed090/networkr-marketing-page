# Production backend API — used when building for production (e.g. Vercel).
# All white-label deployments (networkr-*.vercel.app) use this same backend.
VITE_API_BASE_URL=https://networkr-marketing-pages-api.vercel.app
